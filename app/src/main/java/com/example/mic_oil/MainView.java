package com.example.mic_oil;

import com.arellomobile.mvp.MvpView;

public interface MainView extends MvpView {

        /*
        static void setFragment1(FragmentAndroid1 fragment1, FragmentManager fragmentManager) {
                MainActivity.fragmentTransaction = fragmentManager.beginTransaction();
                MainActivity.fragmentTransaction.replace(R.id.container, fragment1);
                MainActivity.fragmentTransaction.commit();
        }
        */

}
